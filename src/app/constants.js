export const constants = {
    WIDTH: window.innerWidth,
    HEIGHT: window.innerHeight,
    VIEW_ANGLE: 60,
    ASPECT: window.innerWidth / window.innerHeight,
    NEAR: 1,
    FAR: 1000,
    CANVAS: document.getElementById('debug-canvas'),
    WATER_FLOW: '/assets/Water_1_M_Flow.jpg',
};
